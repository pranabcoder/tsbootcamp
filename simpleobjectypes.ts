// Define a type for the person object
type Person = {
    name: string;
    age: number;
};

// Create a person instance with the type specified
const person: Person = {
    name: 'Mark',
    age: 30
};

// Log the person's name to the console
console.log(person.name);
